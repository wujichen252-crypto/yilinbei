# shell 脚本与 CI 配置必须以 LF 存储。本机 core.autocrlf=true，工作区检出时
# 会被转成 CRLF；deploy.sh 一旦带 CRLF 到 Linux，bash 会报
# "bad interpreter: /usr/bin/env bash^M" 而完全无法执行。这里显式钉死行尾。
*.sh    text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
